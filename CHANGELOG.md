# Chrome 1.3.0 / macOS 0.10.0

- 产品显示名、应用包和发布文件统一为 VideoFetch。
- 保留 macOS 配置身份和 Chrome 扩展权限声明，支持覆盖原目录升级。

# 更新记录

## v0.9.0 — 2026-09-09

本次组合发布包含 macOS 助手 0.10.0 和 Chrome 扩展 1.3.0。

- 新增个人学术研究专用许可，明确禁止商业使用；第三方组件仍适用自身许可。
- 扩展提供页面域名、媒体 / CDN 域名设置，按来源精确匹配并按需请求权限。未配置页面不能开始捕获。
- 课程元数据同样受域名配置约束；补片请求不跟随重定向。
- 扩展输出子目录可设置；macOS 输入目录和最终 MP4 目录可选择并持久化。
- 拒绝相同或互相包含的输入 / 输出目录，转换期间禁止修改目录。
- 构建脚本自动定位 macOS SDK；重新生成两端安装包，附带许可文件。
- 保留现有 FFmpeg 文件。

验证：9 项扩展回归检查；macOS 目录持久化、路径重叠及符号链接检查；Apple Silicon 编译、签名检查和 ZIP 完整性检查。

本次未进行真实课程网站的完整播放及下载验收。应用支持 macOS 13+、Apple Silicon，采用本地临时签名，未公证。
